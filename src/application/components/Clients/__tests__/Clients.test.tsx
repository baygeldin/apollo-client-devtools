describe("Clients component tests", () => {
  describe("No clients", () => {
    it.todo("should show an empty list");
  });

  describe("With clients", () => {
    it.todo("should show list of clients in the sidebar");

    it.todo("should set the active client on click");
  });
});
